# E2E — Agent Instructions
Before running the e2e test (`run-devnet.sh`), start a CKB node by running `start.sh`. If the `ckb` binary has been updated, kill all CKB processes and run `start.sh` again.
